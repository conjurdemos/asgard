#!/usr/bin/env ruby
#
# Configure Conjur docker container for web services
#

require File.expand_path('../docker', __FILE__)

CONTAINER_CONFIG = '/etc/conjur.container'

if File.file? CONTAINER_CONFIG
  ContainerConfig.load CONTAINER_CONFIG
  puts "Conjur web service container configured."
else
  exit 1
end
